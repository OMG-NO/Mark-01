<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowAnimationStyle">@style/activityAnim</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="activityAnim" parent="@android:style/Animation.Activity">
    <item name="android:activityCloseEnterAnimation">@anim/activity_closeenter_anim</item>
    <item name="android:activityCloseExitAnimation">@anim/activity_closequit_anim</item>
    <item name="android:activityOpenEnterAnimation">@anim/activity_openenter_anim</item>
    <item name="android:activityOpenExitAnimation">@anim/activity_openquit_anim</item>
    </style>

</resources>
